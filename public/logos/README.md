Coloca aqui el logo principal con este nombre:

- logo-pietra.png

La pagina lo muestra dentro de un emblema circular con borde y adornos dorados, pensado para logos PNG con fondo negro.

Tambien puedes usar otra extension, pero si cambias el nombre del archivo actualiza la variable `logoUrl` en `src/pages/index.astro`.
